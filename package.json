{
  "name": "codex-model-router",
  "version": "0.5.1",
  "private": true,
  "type": "module",
  "description": "Extensible local model router for Codex and Cursor",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "check": "node scripts-check.mjs",
    "test": "node --test --test-timeout=600000 test/*.test.mjs"
  },
  "dependencies": {
    "proper-lockfile": "4.1.2",
    "undici": "8.10.2"
  },
  "devDependencies": {
    "playwright": "1.62.1"
  }
}
